New feature in data managment view to build a staff. Allows you to create a new staff and set each table value manually (this will not need to be able to assign classes).
Similary I would like to add another feature to make a new camper, this will need to be able to assign classes. The class assignment should be functional mirror of the swap tool, but it is always swapping null -> new class

Add "re-build" function on counselor scheduler that allows counselors to be re-scrambled for either sports, or enrichment. Only counselors in those classes should be affected.

On server restart I always need to re-sync the classes from the data managment tab for the period 5 enrichment classes to populate on the scheduler tool. How can this be avoided?